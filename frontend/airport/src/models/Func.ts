export class Function {
  id: number;
  minimumSalary: number;
  name: string;

  constructor(id: number, minimumSalary: number, name: string) {
    this.id = id;
    this.minimumSalary = minimumSalary;
    this.name = name;
  }
}
